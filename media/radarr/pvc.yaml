---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-pv-claim
  labels:
    app: radarr
spec:
  storageClassName: rook-ceph-block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
