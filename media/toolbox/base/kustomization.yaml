---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
- deployment.yaml
configMapGenerator:
- files:
  - yt-dlp.conf
  name: toolbox-config
replacements:
- source:
    fieldPath: metadata.name
    kind: Deployment
    name: toolbox
  targets:
  - fieldPaths:
    - metadata.name
    options:
      delimiter: '-'
      index: 1
    select:
      kind: ServiceAccount
