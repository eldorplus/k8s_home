---
apiVersion: apps/v1
kind: CronJob
metadata:
  name: toolbox-cron
  namespace: media
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: toolbox
            command: ["/bin/bash"]
            args:
            - --login
            - -x
            - /media/youtube/scripts/ytarchive-to-ytdlp.sh
            env:
            - name: HOME
              value: /home/user
